name: web

environment:
  sdk: '>=2.2.0 <3.0.0'

dependencies:
  grpc: ^2.1.3
  intl: ^0.16.0
  protobuf: ^1.0.1

dev_dependencies:
  build_runner: ^1.7.2
  build_web_compilers: ^2.7.0
  pedantic: ^1.9.0
